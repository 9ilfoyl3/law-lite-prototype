# 页面清单

> 版本：v1.17
> 更新日期：2026-07-31
> 说明：本文档登记原型系统所有页面，与 `pages/` 目录保持 1:1 映射。
> 合并说明：v1.8 由 `docs/页面清单.md`（v1.0 旧版，非案件模块为最新）与 `docs/V1.1.0/页面清单.md`（v1.7 案件模块最新设计）合并而成。案件相关页面（3/3-A/3-B/6-A/A-3）采用 V1.1.0 设计，其他模块保持旧版内容。
> v1.17 更新：① 文书详情页顶部移除打印/下载按钮，仅保留文书精修；② 历史文书弹窗查看按钮统一跳转 document-detail.html（不再打开预览小窗口）；③ 全部文书面板移除"重新生成"按钮。
> v1.16 更新：workflow 编辑弹框匹配案由选择器移至匹配案字上方；案字选择器按已选案由联动过滤（仅展示已选案由所属大类允许的案字白名单并集）。
> v1.15 更新：文书类型管理页 workflow 子表格调整为仅展示一步生成型；内置默认从"1 分步型 + 1 一步型"改为"仅 1 个一步生成型"（全兜底）；新增"删除 workflow 时校验同类型至少保留 1 个一步生成型"。
> v1.14 更新：① 案件管理页移除前端Token超限判断描述，新增批量任务入口徽章+进度面板；② 案件文件页新增流式输出、要件侧边抽屉描述；③ 管理后台案件管理改为可编辑模式+多承办人；④ 系统初始化配置页新增批量队列配置；⑤ 登记遗留页面 doc-generation.html（保留不作入口）。
> v1.13 更新：新增「文书精修」页面（pages/document-polish.html），独立页面承载对话式精修，保存为新版本。
> v1.12 更新：移除独立的「文书管理」页面（pages/documents.html）登记与各页面的「文书」导航入口；文书能力统一收口至案件模块（案件列表/案件文件/文书详情），documents.html 文件保留但不再作为入口暴露。
> v1.11 更新：① 新增管理后台「法院维护」「部门维护」两个页面；② 同步文书类型管理（移除图标/步骤序列/来源列、新增状态列）、要件管理（补案字适配、状态列）、文书模板管理（移除案由关联）、提示词管理（移除全局恢复默认、新增历史版本）的描述。
> v1.10 更新：新增用户侧「我的模板」「我的提示词」两个页面；新增管理后台「文书类型管理」页面（含 workflow 子配置）。
> v1.9 更新：案件管理行更新（移除状态/归档，增加改承办人）；新增「文书模板管理」「提示词管理」两个管理后台页面。

---

## 一、用户侧页面（pages/）

| 页面名称 | 文件路径 | 功能简述 | 状态 |
|---------|---------|---------|------|
| 登录页 | [pages/login.html](pages/login.html) | 系统入口，支持普通用户/管理员身份切换登录 | 待开发 |
| AI聊天（核心工作区） | [pages/chat.html](pages/chat.html) | 自然语言交互入口，支持任务发起、处理状态可视化、文档预览 | 待开发 |
| 历史任务 | [pages/tasks.html](pages/tasks.html) | 按任务类型展示历史任务记录，与普通AI聊天会话分离 | 待开发 |
| 案件管理 | [pages/cases.html](pages/cases.html) | 案件列表（按更新时间倒序，支持列配置和横向滚动，含案字列）；案由树与案字按法院/检察院/司法局分别配置；点击名称新标签页打开案件文件页；点击文件数量查看 文件解析状态；操作栏精简为「文件上传」「生成文书」+「更多」下拉（历史文书/编辑/删除）；首列标题「案件名称」；勾选案件后浮动栏支持「批量生成」「删除/批量删除」；删除案件时可选仅清空原始材料或彻底删除案件；点击「生成文书」弹出配置弹框，用户选择文书类型、文书模板并填写提示词后跳转案件详情页自动预填配置+勾选全部材料+触发生成；前端不再做Token超限前置判断（移除前端Token估算），改由workflow返回超限异常时展示决策辅助弹框；顶部栏新增「批量任务」入口按钮（带处理中数量徽章），点击展开进度面板查看每个案件状态；批量生成改为异步队列执行不阻塞列表页操作；案由类型筛选随业务系统动态切换；已移除案件状态筛选项；司法局场景为行政复议业务 | ✅ 已实现 |
| 案件文件 | [pages/case-files.html](pages/case-files.html) | 独立页面仅展示单个案件中已完成解析的材料列表；支持单选/全选、批量删除与批量发送至问答对话框；文件行悬停显示「发送至对话框」；删除材料时可选择「仅删除原始材料（保留已生成文书）」或「同时删除原始材料及已生成文书」；支持重命名、下载、预览单个文件；页面顶部提供文件问答输入区，可选择文件并输入法律问题进行问答；文书生成改为流式输出展示（含进度指示条：已输出字数+预计剩余时间+分段进度，不可取消，完成后自动渲染格式化文书）；一步/分步Tab切换时若已选材料弹确认框；材料树底部新增「本案要件 (N)」入口按钮，点击滑出右侧抽屉展示要件列表（标准/我的/个案三来源标注）；分步生成仅裁判文书类型展示，步骤序列硬编码6步（案件信息→原告诉请→被告答辩→争议焦点→事实认定→裁判结果），步骤间依赖必填/选填拦截；生成结果区头部仅保留【文书精修】【重新配置】两个按钮，无编辑tab、无底部操作栏 | ✅ 已实现 |
| 文书详情 | [pages/document-detail.html](pages/document-detail.html) | 已生成文书的独立详情展示页；全屏布局：左侧 1/4 展示案件材料树（高亮该文书生成时使用的已选材料），右侧 3/4 展示文书内容；顶部仅保留【返回列表】+【文书精修】按钮（V1.1.13 移除打印/下载按钮）；历史文书弹窗/全部文书面板的"查看"统一跳转本页 | ✅ 已实现 |
| 文书精修 | [pages/document-polish.html](pages/document-polish.html) | 文书精修独立页面：左侧文书内容展示区（可编辑）+ 右侧对话式精修区；通过 URL 参数 `caseId` + `versionId` 加载对应版本；展示上下文（案件/文书类型/模板/生成方式/模型）；支持对话精修、撤销、保存为新版本（type='polish'，polishedFrom 标记原版本）；返回案件按钮 + 未保存二次确认 | ✅ 已实现 |
| 知识库 | [pages/knowledge.html](pages/knowledge.html) | 全员公开库与个人知识库的浏览与管理 | 待开发 |
| 个人设置 | [pages/settings.html](pages/settings.html) | 账户信息、全员公开库管理（管理员权限） | 待开发 |
| 我的要件 | [pages/my-elements.html](pages/my-elements.html) | 按案由维护用户自定义要件（含案字适配：案由级勾选适配案字 + 要件级适用案字），生成文书时与标准要件一起作为可选上下文；入口位于案件列表页顶部（与「我的模板」「我的提示词」并列），案件详情页「引入案由要件」弹框内另设「管理我的要件」链接（携带 `cause` 参数定位案由） | ✅ 已实现 |
| 我的模板 | [pages/my-templates.html](pages/my-templates.html) | 用户侧个人文书模板维护，关联文书类型与案由；文书生成时与系统/管理后台模板合并展示 | 🚧 开发中 |
| 我的提示词 | [pages/my-prompts.html](pages/my-prompts.html) | 用户侧个人「提示词」提示词维护；文书生成时与系统/管理后台提示词合并展示 | 🚧 开发中 |

---

## 二、管理后台页面（pages/admin/）

| 页面名称 | 文件路径 | 功能简述 | 状态 |
|---------|---------|---------|------|
| 控制台 | [pages/admin/dashboard.html](pages/admin/dashboard.html) | 系统核心数据概览、快捷操作、系统通知 | ✅ 已实现 |
| 用户管理 | [pages/admin/users.html](pages/admin/users.html) | 用户全生命周期管理、批量导入/删除；含法院+部门筛选联动、所属法院/所属部门列 | ✅ 已实现 |
| 法院维护 | [pages/admin/court-tree.html](pages/admin/court-tree.html) | 超管专用：维护法院上下级组织树（高级/中级/基层），为每个法院分配法院管理员账号；法院管理员不可见 | ✅ 已实现 |
| 部门维护 | [pages/admin/department.html](pages/admin/department.html) | 法院管理员维护本法院部门组织（部门仅作人员重名识别标签，不做数据权限划分）；同法院下部门名唯一校验；删除前校验部门下是否有用户 | ✅ 已实现 |
| 案件管理 | [pages/admin/cases.html](pages/admin/cases.html) | 全院视角案件管理（可编辑模式）：业务系统切换按钮（默认法院）、筛选栏（搜索/部门/承办人/年份）、案件表格 10 列（含动态计算的部门、文书数，不含业务系统列与状态列）、批量改承办人/批量删除、行内编辑（弹框编辑结构化信息，与用户侧 cases.html 编辑按钮交互一致）/改承办人/删除/恢复、分页；承办人管理支持多选（一个案件可有多个承办人，支持多人协作），承办人候选来自本法院活跃用户列表；修改后普通用户侧「仅本人案件」= 当前用户为承办人之一的案件 | ✅ 已实现 |
| 系统运行报告 | [pages/admin/system-report.html](pages/admin/system-report.html) | 静态周报/月报/自定义报告，支持导出PDF | ✅ 已实现 |
| 知识库分类管理 | [pages/admin/knowledge-base.html](pages/admin/knowledge-base.html) | 全员公开库分类体系维护 | ✅ 已实现 |
| 知识库文档管理 | [pages/admin/knowledge-documents.html](pages/admin/knowledge-documents.html) | 知识库文档上传、状态管理、解析索引 | ✅ 已实现 |
| 案由管理 | [pages/admin/cause-management.html](pages/admin/cause-management.html) | 三级案由层级结构维护，上线/下线管理 | ✅ 已实现 |
| 文书类型管理 | [pages/admin/doc-types.html](pages/admin/doc-types.html) | 维护各业务系统文书类型（文书模板/提示词的父级分类）+ workflow 子配置：左侧业务系统切换 + 右侧类型表格 CRUD（列：展开/类型名称+内置自定义徽章/模板数/提示词数/workflow数/状态/操作）；每行可展开显示其下 workflow 列表（仅展示一步生成型，workflow id 下拉 + 类型 + 匹配案由，步骤序列由 agentflow 平台决定前端不配置）；每个文书类型默认有且仅有 1 个内置"一步生成型" workflow（全兜底），不生成内置分步生成型（分步型由前端 stepConfigsByOrg 硬编码提供，仅裁判文书有）；workflow 编辑弹框中匹配案由选择器位于匹配案字上方，案字选择器按已选案由联动过滤（仅展示已选案由所属大类允许的案字白名单并集，未选案由时展示全部案字）；内置类型可编辑名称另存为自定义覆盖；支持启用/停用（停用后用户侧不展示）；删除前校验是否被模板/提示词引用；删除 workflow 时校验"同类型至少保留 1 个一步生成型"；持久化 localStorage.adminDocTypes / localStorage.adminWorkflows；用户侧 mergeAdminDocTypes 跳过 enabled===false | ✅ 已实现 |
| 要件管理 | [pages/admin/element-presets.html](pages/admin/element-presets.html) | 维护各业务系统的标准要件：左侧案由列表（来自 causeTreeDataByOrg，含置顶/搜索）+ 右侧要件表格（要件名/说明/问题/适用案字/来源/状态/操作）；顶部「适配案字」选择器；内置要件可编辑另存为自定义覆盖；支持启用/停用（停用后用户侧 filterElementsByCaseWord 过滤）；支持为某案由新增/删除要件；持久化 localStorage.adminElementPresets；用户侧 getElementPresets 优先读取覆盖项 | ✅ 已实现 |
| 文书模板管理 | [pages/admin/doc-templates.html](pages/admin/doc-templates.html) | 维护各业务系统文书模板：左侧文书类型列表 + 右侧模板表格；模板直接关联文书类型（不再单独维护案由，继承文书类型的案由匹配）；支持模板正文编辑；持久化 localStorage.adminDocTemplates；用户侧按所属文书类型直接展示 | ✅ 已实现 |
| 提示词管理 | [pages/admin/prompt-templates.html](pages/admin/prompt-templates.html) | 维护提示词模板：左侧文书类型列表 + 右侧提示词列表；按业务系统×文书类型分组；支持历史版本记录（保留最近 10 条，一键恢复）；持久化 localStorage.adminPromptTemplates；用户侧 renderReqTemplates 优先读取 | ✅ 已实现 |
| 风控与审计 | [pages/admin/risk-audit.html](pages/admin/risk-audit.html) | 敏感词库（预设只读+追加）、审计日志（含拦截记录），支持导出PDF | ✅ 已实现 |
| 系统初始化配置 | [pages/admin/settings.html](pages/admin/settings.html) | 部署时一次性配置，配完锁定（需超管解锁）；含批量文书队列允许个数配置（默认10，最小1） | ✅ 已实现 |
| 模型配置与用量 | [pages/admin/model-management.html](pages/admin/model-management.html) | 模型部署清单（按法院）、场景模型配置、配额与用量（含Token阈值）；**V1.1 菜单入口已隐藏，代码文件保留** | ✅ 已实现 |

---

## 三、任务入口设计说明

### 3.1 当前版本（V1.0）开放任务入口

Chat 页面任务选择器中，**仅开放以下 3 个办案任务入口**，其余任务做隐藏处理：

| 任务名称 | 对应核心场景 | 入口状态 |
|---------|------------|---------|
| 裁判文书 | 裁判文书生成 | **开放** |
| 庭审提纲 | 庭审提纲生成 | **开放** |
| 法条适用 | 法条检索与校验 | **开放** |

### 3.2 当前版本隐藏任务入口

以下任务在 V1.0 不做独立任务页面，入口隐藏，用户不可选择：

| 任务名称 | 隐藏原因 | 后续规划 |
|---------|---------|---------|
| 类案检索 | 非核心用户场景，底层能力支撑文书生成 | 作为底层能力嵌入文书生成流程 |
| 争议焦点 | 非核心用户场景，已融入庭审提纲 | 第二阶段独立模块 |
| 文本总结 | 通用任务，非办案核心 | 作为通用能力保留在后台 |
| 文本润色 | 通用任务，非办案核心 | 第二阶段"文风润笔"智能体替代 |
| 票据汇总 | 通用任务，非办案核心 | 第二阶段"票证智汇"智能体替代 |

### 3.3 尚未设计任务页面的核心能力

以下核心场景的能力在 V1.0 通过 Chat 自然语言交互触发，**暂无独立任务页面**：

| 核心场景 | 当前触发方式 | 独立页面规划 |
|---------|------------|------------|
| 文书质量评查 | 自然语言输入"评查这份文书" | 后续迭代评估 |
| 案由智能推荐 | 自然语言输入案情描述，系统自动推理 | 后续迭代评估 |
| 证据梳理 | 自然语言输入"梳理本案证据" | 后续迭代评估 |
| 知识问答 | 自然语言直接提问 | 保持Chat入口，不独立页面 |

---

## 四、页面结构规范

### 4.1 用户侧统一布局

所有用户侧页面采用统一的 **左侧导航（64px图标栏） + 顶部栏（64px） + 内容区** 布局：

```
┌─────────────────────────────────────────┐
│  侧边栏 │        顶部栏（页面标题）        │
│  64px   ├────────────────────────────────┤
│         │                                │
│  💬 聊天 │         内容区                 │
│  ✅ 任务 │         （自适应）              │
│  📁 案件 │                                │
│  📚 知识 │                                │
│         │                                │
└─────────┴────────────────────────────────┘
```

### 4.2 管理后台统一布局

所有管理后台页面采用统一的 **侧边栏（260px） + 顶部导航（64px） + 内容区** 布局：

```
┌─────────────────────────────────────────┐
│  侧边栏    │        顶部导航栏            │
│  260px     ├─────────────────────────────┤
│            │                             │
│  Logo      │         内容区              │
│  ────────  │         （自适应）           │
│  控制台    │                             │
│  用户管理  │                             │
│  ...      │                             │
│            │                             │
└────────────┴─────────────────────────────┘
```

---

## 五、遗留页面（保留不作入口）

以下页面文件保留在项目中，但不再作为任何导航入口暴露：

| 页面名称 | 文件路径 | 说明 |
|---------|---------|---------|
| 文书管理（旧） | [pages/documents.html](pages/documents.html) | v1.12 移除入口，文书能力统一收口至案件模块 |
| 分步可视化（旧） | [pages/doc-generation.html](pages/doc-generation.html) | 原分步可视化页面，列表页「生成文书」不再跳转该页，能力收口至 case-files.html |

---

*文档结束*
