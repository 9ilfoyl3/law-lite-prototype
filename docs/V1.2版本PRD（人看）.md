# AI辅助办案系统 - V1.2版本产品需求文档（PRD）

> 文档版本：V1.2.15
> 编写日期：2026-09-08
> 产品负责人：产品团队
> 文档密级：内部机密
> 上游文档：[V1.1版本PRD（人看）.md](V1.1版本PRD（人看）.md)（其版本记录表含 V1.1.1 / V1.1.2 调整记录）

***

# 一、版本记录

| 版本号 | 修订人  | 修订日期       | 修订描述 |
| :--- | :--- | :--------- | :--- |
| V1.2.15 | 产品团队 | 2026-09-10 | **OCR/分块查看改为独立页面（修正 V1.2.13 弹框方案）+ 清理用户侧 + 修复操作列**：① 管理端【OCR/分块】由「附件式弹框」改为**独立页面** `pages/admin/ocr-chunk-view.html`（复用管理端侧边栏 + 面包屑「首页 / 案件管理 / OCR/分块查看」，主体左材料列表 / 右 OCR 内容与分块，布局与交互同 V1.2.13，含 3 粒度 + 重新切片；带 `?caseId=` 参数进入，数据层复用 `case-data.js`）；② **移除用户侧案件详情页**（`pages/case-files.html`）V1.2.12 残留的 OCR Tab（`#resultModeTabs` / `#ocrChunkPane` 及配套 CSS/JS），用户侧不再有 OCR 入口；③ **修复管理端操作列按钮显示不全**——操作列由 180px 加宽至 268px，`action-cell` 增加 `flex-wrap:nowrap` 防止挤压换行；④ 顺带修复 `case-files.html` 中法条库弹窗后多余的一个 `</aside>` 闭合标签。详见 9.9 |
| V1.2   | 产品团队 | 2026-09-08 | 生成文书「引入要件」交互重构：有可用要件时统一先弹轻量询问弹框；选择引入后不再弹小窗口，直接复用「本案要件」抽屉展示与编辑答案；抽屉底部新增生成模式专属【确认生成（引入 N 条要件）】按钮 |
| V1.2.1 | 产品团队 | 2026-09-08 | 要件抽屉交互增强（对应 2026-09-07 演示评审清单第 6/7/8 项）：① 一键生成取消整体遮罩、改为逐条并行呈现，任务运行期间已生成答案要件的【编辑】【重新生成】【内容优化】放开可用；② 未答要件新增单条【生成答案】按钮；③ 标准/我的要件支持个案移除（复用个案要件删除按钮样式，仅当前案件生效，不影响后台案由模板与个人预设，答案与优化记录联动清理）；④ 抽屉底部新增「恢复默认要件」入口，一键恢复本案已移除要件。详见第八章 |
| V1.2.2 | 产品团队 | 2026-09-08 | 新建案件流程极简（对应 2026-09-07 演示评审清单第 12 项）：① 新建弹窗仅保留核心上传入口，移除案件名称必填输入与「补充结构化信息」7 字段折叠区及冗余说明文字；沿用原有 0 文件建案能力；② 上传建案后不跳转、留在案件列表页，案件名称处增加「新」标识（编辑保存后消除）；材料进入真实「解析中」状态（每文件独立 2-3 秒 mock 解析）；③ 不做确认浮层：详情页以提示条提示解析状态——解析中「正在从材料中提取案件信息」（浅蓝，对齐实际系统截图），完成后提示确认修改并可一键跳转；信息修改入口为列表页「编辑」按钮（现有编辑弹框，mock 识别结果预填，对齐实际系统截图，含案字/承办人）；④ 列表页「原告/被告」动态列名统一改为「当事人」。详见第九章 |
| V1.2.3 | 产品团队 | 2026-09-08 | 第九章修订：① 建案必须 ≥1 个文件（取消 0 文件建案），无文件点「保存案件」toast 提示 + 上传区闪烁引导、按钮弱化置灰；② 上传类型与大小限制对齐实际系统截图：PDF / DOC / DOCX / JPG / JPEG / PNG 及 ZIP 整卷压缩包，单个材料 ≤50MB、压缩包 ≤1000MB，ZIP 胶囊提示「上传 ZIP 可一次性归入整卷卷宗」；文件列表图标按类型区分；③ 新建弹窗主按钮文案固定「上传并解析」（不做「保存案件/上传并解析」切换，无文件时弱化置灰）；④「我的模板」「我的指令」「我的要件」页隐藏法院/检察院/司法局业务系统切换，固定默认法院（含 URL 不再传 org 参数，避免 localStorage 残留值锁定非法院视图） |
| V1.2.4 | 产品团队 | 2026-09-10 | 第九章修订：新建弹窗恢复案件名称必填输入框（红色星标）——上传后自动填充首个文件名（去扩展名），用户可修改；移除全部文件自动清空、重新上传自动回填（用户手动编辑过则不覆盖）；为空时点「上传并解析」toast「请填写案件名称」+ 输入框聚焦红色闪烁，不建案。目的：建案前名称对用户可见可改，避免建案后找不到刚上传的案子 |

| V1.2.5 | 产品团队 | 2026-09-08 | 第十章修订（法条分类与库内检索添加）：①「本案法条」按来源分为「AI推荐」（原系统检索，系统按案由自动匹配）与「手动添加」（用户从法条库检索添加）两组，复用要件抽屉分组样式；② 新建全量法条库 `LAW_LIBRARY_ALL`（82 条，按民法典总则/合同/物权/侵权/婚姻家庭继承、民事诉讼法、劳动合同法、公司法、刑法、刑诉法、行政诉讼法分类），AI 推荐改为按案由关键词映射法条 id 列表；③ 抽屉底部【+ 添加法条】打开法条库弹窗：关键词搜索、勾选多条、已添加的置灰防重、底部【确认添加（N 条）】；④ 库内无匹配时可【新建自定义法条】（名称必填/内容选填/分类选填），以「手动添加」来源入库，用于地方法规等库外条款。详见 10.9 |
| V1.2.6 | 产品团队 | 2026-09-08 | 第十章修订（法条生成策略，仅改文档、原型不动）：法条预生成**触发时机**由「进入案件详情页」调整为「**案件上传后**后台异步预生成」——与结构化信息提取（V1.2.2 mock 解析）同源、同生命周期，案件上传完成即触发；用户进入对应环节时未生成完成则等待、已生成则直接展示。详情页/列表页加载时仅负责「检测并续接」未完成任务，不再作为触发点。详见 10.3.1 / 10.6 |
| V1.2.14 | 产品团队 | 2026-09-10 | 第十章新增 10.12（文书排版，对应 2026-09-09 开发对齐清单第 5 项）：详情页右上角【历史文书】左侧新增【文书排版】按钮。本期默认只做「标题+统一正文、靠左」的脚本流式渲染（案号/签章/落款后置，保证生成速度），用户需要全量格式时**手动触发**排版；排版为脚本匹配（补案号行 + 落款签章区 + 标题居中/首行缩进/宋体/院印），不使用大模型，幂等、可重复排版。详见 10.12 |
| V1.2.13 | 产品团队 | 2026-09-10 | **21 项方案（管理端，形态已由 V1.2.15 废止）**：管理端案件管理（`pages/admin/cases.html`）**操作列**新增【OCR/分块】按钮（与编辑/改承办人/删除并列），点击弹出排错弹窗——**左侧案件材料列表**（带解析状态圆点，可切换）、**右侧 OCR 内容与分块**（信息条 +【识别全文】/【分块预览】子 Tab + 3 种粒度按页/按段/按句 +【重新切片】600ms loading）。修正前两版偏差：V1.2.11 放文件数列（列宽 100px 挤压）已撤；V1.2.12 误改详情页（右栏默认 `flex:0 0 0%` 折叠导致不可见）已废弃。**注：弹框形态因用户反馈「不要附件式弹框」已于 V1.2.15 改为独立页面** |
| V1.2.12 | 产品团队 | 2026-09-08 | 21 项改放详情页（修正 V1.2.11 位置）：详情页右栏（result-col）头部加模式 Tab【生成文书 / OCR 排错】，切到 OCR 排错后右栏展示选中文件的「识别全文」与「分块预览」两个子 Tab，3 种粒度（按页/按段/按句）+ 重新切片。文件来源：左侧材料树选中的文件，无选中时显示已解析成功的文件。V1.2.11 管理端方案已撤回 |
| V1.2.11 | 产品团队 | 2026-09-08 | 第九章新增 9.9（OCR/分块查看入口，对应清单 21，**本版本已撤回**——用户调整为详情页方案，见 V1.2.12） |
| V1.2.10 | 产品团队 | 2026-09-08 | 第九章修订（去修改不再跳页）：详情页提示条【去修改】由"跳回案件列表页并打开编辑弹框"改为**在详情页内直接弹出编辑弹框**，不跳页、不丢上下文。新增案件信息编辑弹框公共组件 `js/case-edit-dialog.js`（DOM 动态注入 + 案由树 + 保存逻辑，依赖均为两页共用的 `case-data.js`），详情页与列表页共用；保存后回调刷新详情页提示条与案件信息。详见 9.3.2 / 9.8 |
| V1.2.9 | 产品团队 | 2026-09-08 | 第十章新增 10.11（法条交互回退与入口区分）：① **取消** 10.10 的「核心依据 / 可补充依据」分层 UI——系统认为的核心也许是错的，用户手动添加的才可能是核心，不应由系统替用户判断重要性；AI 推荐合并为一组展示，`tier` 字段退化为仅用于推荐排序；② AI 推荐法条改为**默认全不选**，统计条右侧提供【全选】【清空】快捷操作（手动添加仍默认勾选）；③ **已勾选法条置顶展示**并加浅色底标识，勾选/取消后即时重排；④ 底部按钮文案【添加法条】【重新检索】改为**【手动检索】【AI推荐】**（人工 vs AI 语义对立）；⑤ 材料树两个入口做区分：法条（必确认）主色实心 + 状态徽标（检索中 / 待确认 / 已确认 N 条），要件（选填）弱化样式 + 「选填」灰标。详见 10.11 |
| V1.2.8 | 产品团队 | 2026-09-08 | 第十章新增 10.10（法条分层与默认勾选策略）：① AI 推荐法条分为「核心依据」（实体法核心条款，2-3 条，**默认勾选**）与「可补充依据」（程序法/通用条款，**默认不勾**），抽屉内与「手动添加」并列共三组；② 抽屉顶部新增后果说明条「勾选的法条将写入文书『法律适用依据』章节，未勾选的不写入」；③ 确认按钮改为「确认法条并生成文书（N 条写入文书）」；④ AI 推荐数量收敛至每案由 core 2-3 条 + optional ≤5 条、**总数 ≤8 条**（原借贷 10 条）。默认勾选由"减法"改为"加法"。详见 10.10 |
| V1.2.7 | 产品团队 | 2026-09-08 | 第十章修订（重新检索语义）：【重新检索】按钮**仅重新生成 AI 推荐法条，保留手动添加的法条**（原逻辑清空全部含手动，提示文案有误）。案由变更时仍清空全部（含手动，因案由已变）。确认文案改为「重新检索将按当前案由重新生成 AI 推荐法条，手动添加的法条将保留，是否继续？」。详见 10.3.3 / 10.4.2 |
| V1.2.4 | 产品团队 | 2026-09-08 | 文书生成流程重构（对应 2026-09-07 演示评审清单第 1/2/3/4 项）：① 工作流拆为「法条确认 → 文书生成」两段串行，一次动作只允许一次模型调用；② 案件上传后后台异步预生成法条，用户点到该环节时未完成则等待、已完成则直接查看调整；③ 新增「本案法条」抽屉（复用本案要件抽屉组件与交互），法条可增/删/改/勾选，未确认不得进入文书渲染；适用于全部文书类型；④ 渲染方案弃用大模型渲染，改为脚本匹配式流式实时渲染——标题 + 统一字体正文、整体靠左，案号/签章等特殊格式本期后置。详见第十章 |
***

# 二、调整背景与目标

## 2.1 背景

V1.1 现行交互中，"要件答案的日常维护"与"生成文书时的答案确认"是两套割裂的交互：

| 场景 | 现行载体 | 能力 |
| :--- | :--- | :--- |
| 日常维护（材料树「本案要件」入口） | 本案要件抽屉 | 逐条答案展示/编辑、单条重新生成、内容优化（3 轮上限）、优化记录回填、勾选框、一键生成 |
| 生成时确认·有答案（原路径 A） | 大文本框确认弹框 | 整段纯文本编辑；按钮【取消】【不引入要件】【确认生成】 |
| 生成时确认·无答案（原路径 B） | 三步弹框串联 | ① 确认是否引入 → ② 勾选要件 → ③ 大文本框（含预生成加载态） |

## 2.2 现状问题

1. **编辑体验割裂**：同一份要件答案，日常维护用抽屉、生成确认用大文本框，用户需重复学习两套交互
2. **能力不对等**：抽屉具备的单条重新生成、内容优化、优化记录回填在生成确认时不可用；大文本框只能整段手改
3. **脆弱的文本解析**：大文本框确认后需用正则将纯文本按 `【要件名】问题：…答案：…` 拆回结构化数据，用户改动格式后解析易失败
4. **路径 B 链路长**：三连弹框，操作成本高

## 2.3 调整目标

1. 生成确认与日常维护统一复用「本案要件」抽屉，一套交互
2. 要件上下文以结构化数据传递给 workflow，消除纯文本拼接 + 正则解析链路
3. 原路径 B 由"三步弹框"缩短为"轻量询问 + 抽屉"
4. 抽屉内编辑的答案实时持久化，生成前修改的内容留档，日常模式同样可复用

***

# 三、调整范围

## 3.1 涉及页面与功能

| 页面 | 模块 | 功能点 |
| :--- | :--- | :--- |
| pages/case-files.html（案件文件页） | 文书生成 | 5.2 功能3 一步生成（案由要件引入规则）、5.2 功能5 引入要件（路径 A/B 交互重构）、分步生成完成全部步骤后「生成文书」按钮的同款联动 |

## 3.2 保持不变

- **触发条件**：文书类型为判决书（judgment）且该案由存在可用要件（标准/我的/个案任一数量 > 0），延续 v1.27 / v1.46 规则
- **无可用要件**：直接生成，不弹任何框（原路径 C）
- **双入口适用**：一步生成、分步生成「生成文书」按钮均按本流程处理，延续 v1.49 规则
- **列表页入口**：跳转详情页后自动引入全部可用要件（缺失答案自动 mock 生成）并自动生成，全程不弹框——本次不调整
- **抽屉日常模式**（材料树「本案要件」入口打开）全部现有功能不变
- 要件三色来源标签（标准=蓝 / 我的=绿 / 个案=橙）、「一键生成」、内容优化 3 轮上限、优化记录回填等能力不变

***

# 四、调整后交互方案（TO-BE）

## 4.1 整体流程

```
点「生成文书」（一步生成 / 分步生成最终按钮）
        │
        ├─ 非判决书 或 无可用要件 ──────────→ 直接开始流式生成
        │
        └─ 判决书 且 有可用要件
                │
        轻量询问弹框（复用现有 preElementConfirmModal）
        「检测到 N 个可用要件…是否引入」
                │
        ┌───────┴──────────────┐
  【引入案由要件】        【直接生成】 / X 关闭
        │                      │
        │               直接生成 / 中止回到配置区
        ▼
   本案要件抽屉 · 生成模式（默认全选）
        │
        ├─ 有答案：直接展示，可编辑 / 单条重新生成 / 内容优化
        ├─ 无答案：手动点「一键生成」预生成（含加载态），再编辑
        │
   底部【确认生成（引入 N 条要件）】
        │
        ├─ N ≥ 1：抽屉自动关闭 → 主界面流式输出开始
        ├─ 勾选了要件但均无答案：toast「尚无答案，建议先生成」，不触发生成
        └─ 未勾选任何要件：toast「请先勾选要引入的要件」，不触发生成
```

## 4.2 轻量询问弹框（前置）

- **复用现有 `preElementConfirmModal`**：样式、文案（共 N 个可用要件（标准 X / 我的 Y / 个案 Z）、引入价值说明、耗时提示）沿用现状
- **触发规则变化**：原"有答案（路径 A）不询问、直接弹大文本框"的行为**取消**；只要满足触发条件（判决书 + 有可用要件），一律先弹轻量询问弹框，有答案/无答案不再影响前置流程
- **按钮**：
  - 【引入案由要件】→ 打开本案要件抽屉（生成模式）
  - 【直接生成】→ 不引入要件，直接开始流式生成
  - X / 遮罩关闭 → 中止本次生成动作，回到配置区

## 4.3 本案要件抽屉 · 生成模式

- **唯一来源**：生成文书 → 轻量询问弹框 → 【引入案由要件】。从材料树「本案要件」入口打开为**日常模式**，底部不显示确认生成按钮，行为与现状完全一致
- **默认勾选**：每次经【引入案由要件】进入生成模式时，重置为全部可用要件勾选（不沿用上次取消勾选的状态）
- **复用抽屉现有能力**：逐条答案展示/编辑、单条【重新生成】、【内容优化】（N/3）、优化记录回填、新增个案要件、勾选统计、三色来源标签、「管理我的要件」链接
- **有答案（承接原路径 A）**：直接展示已有答案，用户可编辑后确认生成
- **无答案（承接原路径 B）**：答案区为空，由用户手动点底部「一键生成」为勾选要件批量预生成答案（含现有加载态），完成后可继续编辑；**系统不自动触发预生成**
- **底部操作条**：生成模式下新增主按钮**【确认生成（引入 N 条要件）】**（主色按钮，置于抽屉底部按钮区）；N 为"已勾选且有答案"的要件数，随勾选与答案状态实时联动刷新文案；日常模式不渲染该按钮
- **一键生成加载期间**：【确认生成】按钮禁用，加载结束恢复

## 4.4 确认生成校验规则

| 校验情形 | 行为 |
| :--- | :--- |
| N ≥ 1（存在勾选且有答案的要件） | 关闭抽屉 → 主界面立即开始流式输出；勾选但无答案的要件不纳入上下文 |
| 勾选了要件但均无答案（N = 0） | toast 提示**「尚无答案，建议先生成」**，不触发生成 |
| 未勾选任何要件 | toast 提示「请先勾选要引入的要件」，不触发生成 |

- **部分有答案 / 部分无答案**：仅引入有答案的 N 条（按钮文案中的 N 即该数量）；如需引入更多，可先「一键生成」或单条【重新生成】补齐答案后再确认

## 4.5 关闭与放弃

- 生成模式下点 X / 遮罩关闭抽屉 = **放弃本次生成**，回到配置区，不触发生成
- 抽屉内已编辑/生成的答案已实时持久化（现有机制），关闭不丢失，下次进入（含日常模式）恢复

## 4.6 生成中状态约束

- 确认生成后**抽屉自动关闭**，再开始流式输出
- 流式输出期间材料树「本案要件」入口**禁用**，hover 提示「文书生成中，暂不可修改要件」；输出结束后恢复

## 4.7 数据传递与持久化

- 引入上下文以**结构化数组**传递给生成流程（name / desc / question / answer），范围 = 勾选 ∩ 已有答案
- 废除"纯文本拼接 + 正则拆解"链路（原大文本框确认逻辑）
- 答案持久化沿用 `localStorage.caseElements_${caseId}`，存储结构不变，历史数据无需迁移

***

# 五、移除的旧交互（代码清理清单）

| 移除项 | 说明 |
| :--- | :--- |
| elementContextModal 大文本框弹框 | HTML + JS（showElementContextModal / confirmElementContextModal / skipElementContext / closeElementContextModal / resetElementContextLoading 等）及其预生成加载态（elementContextLoading） |
| elementConfirmModal 勾选要件弹框 | 原路径 B 第二步（含【上一步】【引入要件】按钮组与已选计数）；选择能力由抽屉勾选框承接 |
| 纯文本正则解析逻辑 | confirmElementContextModal 内按 `【要件名】问题：/答案：` 拆块的解析代码 |
| 路径区分辅助函数 | hasAnyElementAnswer / buildElementAnswersFromExisting / elementContextHasPrevious / backToPreElementConfirm 等中间状态管理 |

***

# 六、边界与异常 case

1. **无可用要件**：不弹框直接生成（不变）
2. **历史答案数据**：存储结构未变（caseElementsAnswers 按要件名索引），大文本框时代的既有答案直接可用，无需迁移
3. **流式输出中**：「本案要件」入口禁用，无法打开抽屉
4. **生成模式下新增个案要件**：新增后纳入列表与勾选统计，N 联动刷新
5. **生成模式放弃后再点「生成文书」**：重新走轻量询问弹框；再次进入抽屉时重置为默认全选
6. **一键生成加载中点【确认生成】**：按钮已禁用，无响应；加载完成后自动恢复

***

# 七、验收要点

| # | 验收项 |
| :--- | :--- |
| 1 | 判决书 + 有可用要件，点「生成文书」出现轻量询问弹框，按钮为【引入案由要件】【直接生成】 |
| 2 | 【直接生成】不引入要件立即开始流式输出；X / 遮罩关闭中止，回到配置区 |
| 3 | 【引入案由要件】打开抽屉：默认全选、底部显示【确认生成（引入 N 条要件）】；从材料树入口打开不显示该按钮 |
| 4 | 有答案要件直接展示答案，可编辑 / 单条重新生成 / 内容优化 |
| 5 | 无答案时点「一键生成」出现加载态，完成后答案填充可编辑；加载期间【确认生成】禁用 |
| 6 | N ≥ 1 点【确认生成】：抽屉关闭、流式输出开始、上下文仅含勾选且有答案的要件（结构化数据） |
| 7 | 勾选了要件但均无答案：点【确认生成】toast「尚无答案，建议先生成」，未触发生成 |
| 8 | 未勾选任何要件：点【确认生成】toast「请先勾选要引入的要件」 |
| 9 | 生成模式关闭抽屉（X / 遮罩）：放弃本次生成，回配置区，已编辑答案保留 |
| 10 | 流式输出期间「本案要件」入口禁用并 hover 提示；输出结束后恢复 |
| 11 | elementContextModal / elementConfirmModal 两个旧弹框不再出现，相关代码已移除 |
| 12 | 分步生成完成全部步骤后的「生成文书」按钮同样走新流程 |

***

# 八、V1.2.1 要件抽屉交互增强

> 本章节为 V1.2.1 增量调整，对应 2026-09-07 演示评审《原型待调整清单》第 6/7/8 项。
> 原则：**不改动现有抽屉 UI 版式与组件样式**，仅调整交互逻辑与状态呈现。

## 8.1 调整背景

| # | 问题 | 现状行为 |
| :--- | :--- | :--- |
| 1 | 一键生成期间操作被整体锁死 | 点击「一键生成」后加载遮罩覆盖整个要件列表，直至全部任务结束；期间已生成答案的要件无法【编辑 / 重新生成 / 内容优化】，与"并行生成、出一个是一个"的目标不符 |
| 2 | 要件无法在个案中移除 | 仅「个案要件」有删除按钮；「标准要件 / 我的要件」从案由模板/个人预设复制到案件后无移除入口，个案不需要的要点只能留在列表中干扰 |

## 8.2 调整 1：一键生成并行化，已答要件操作放开

### 8.2.1 交互方案

- **取消整体加载遮罩**：点击「一键生成」后不再用遮罩盖住要件列表，各要件卡片独立呈现生成状态
- **逐条并行生成**：批量任务按要件逐条完成（原型模拟分批延时填充），每完成一条**立即**展示答案并放开该条操作，无需等全部完成
- **已答要件即时可操作**：批量任务开始前已有答案的要件，以及任务中已完成要件，【编辑】【重新生成】【内容优化（N/3）】全程可用；单条操作与批量任务并行不互斥；批量过程中每完成一条仅就地刷新该条卡片，不打断其他条目正在进行的内联编辑
- **未答要件状态**：任务运行期间答案区显示轻量生成中指示（沿用现有 loading 样式风格）
- **单条【生成答案】按钮**：非批量运行时，未答要件答案区显示【生成答案】按钮（✨ 图标，文案与实际系统一致），点击单独生成该条答案（含单条加载态），完成后展示常规操作按钮；批量运行期间该按钮被生成中指示替代
- **底部按钮**：沿用现状——运行期间「一键生成」禁用并显示加载态、「新增个案要件」禁用；生成模式下【确认生成（引入 N 条要件）】保持禁用（V1.2 4.3 节防半成品确认规则不变）
- **完成汇总**：全部完成后 toast「已为 N 项要件生成答案，可直接在列表中修改」（沿用现有文案），底部按钮恢复

### 8.2.2 保持不变

- 一键生成触发逻辑：对全部要件批量生成、无需勾选、完成后自动勾选已生成要件
- 生成模式「确认生成」按钮加载期间禁用规则（V1.2 4.3）
- 抽屉关闭时清理运行中状态（含未完成计时器，已完成答案保留）

## 8.3 调整 2：标准/我的要件个案移除

### 8.3.1 交互方案

- **入口**：标准要件/我的要件卡片标题行右侧新增删除按钮，**复用个案要件现有删除按钮样式**（垃圾桶图标 `case-elements-item-del-btn`），不新增弹窗或菜单
- **点击行为**：直接移除 + toast「已从本案移除"XX"要件，不影响案由模板」（与个案要件删除行为一致，不弹二次确认）
- **生效范围**：**仅当前案件**——后台案由要件配置（标准）、个人要件预设（我的）不受影响；同案由新案件仍复制全量模板
- **联动清理**：移除时一并清理该要件的答案、内容优化记录、勾选状态；「一键生成」不再覆盖生成已移除要件；标签计数（如"标准 16"）与勾选统计联动刷新
- **数据结构**：`localStorage.caseElementsRemoved_${caseId}` 存储已移除要件名数组，加载案由模板时过滤；个案要件维持现有物理删除逻辑不变

### 8.3.2 恢复默认要件

- **入口**：抽屉底部左侧「管理我的要件」链接旁新增「恢复默认要件」文本链接（含图标，样式对齐现有链接风格）；**仅当本案存在已移除的标准/我的要件时显示**，无移除记录不渲染
- **点击行为**：一次性恢复本案全部已移除要件（清空移除记录并重新加载模板）+ toast「已恢复 N 项默认要件」；恢复的要件答案为空（移除时已清理），需重新单条生成或一键生成
- **运行约束**：批量生成运行期间「恢复默认要件」禁用并 hover 提示「生成中，请稍候」，与底部按钮一致

### 8.3.3 移除语义

- 与演示评审结论一致：移除 = 个案内"不引用"，非物理删除模板；删除按钮 tooltip 文案「从本案移除，不影响案由模板」

## 8.4 边界与异常 case

1. 批量生成运行中关闭抽屉：清理运行状态（含全部未完成计时器），已完成答案保留
2. 批量生成运行中点【确认生成】：按钮已禁用，无响应；全部完成后自动恢复
3. 移除要件后点「一键生成」：过滤已移除项，仅对剩余要件生成
4. 移除后同案由新建案件：全量复制模板，不受影响
5. 生成模式下移除要件：底部【确认生成（引入 N 条要件）】的 N 计数联动刷新
6. 移除"我的要件"：仅本案不引用，个人预设库（跨案件复用）不受影响
7. 批量生成运行中移除某条待生成要件：该条跳过生成（不写入答案），其余照常完成，完成计数照常推进
8. 单条生成运行中移除该要件：跳过写入，清理该条计时器
9. 批量生成运行中恢复默认要件：入口已禁用，无响应；全部完成后恢复可用
10. 恢复默认要件后再次移除：走同一移除逻辑，移除记录可反复增删

## 8.5 验收要点（V1.2.1）

| # | 验收项 |
| :--- | :--- |
| 1 | 点「一键生成」：无整体遮罩；未答要件逐条显示生成中，完成后即时出答案 |
| 2 | 批量运行期间，已答要件【编辑/重新生成/内容优化】可点击且正常执行，互不阻塞；某条完成刷新不中断其他条目的内联编辑 |
| 3 | 批量运行期间【确认生成】禁用，全部完成后恢复（生成模式） |
| 4 | 全部完成后底部按钮恢复「一键生成」，toast 汇总生成数量 |
| 5 | 非批量运行时，未答要件显示【生成答案】按钮，点击单条生成（含加载态），完成后展示常规操作按钮；批量运行期间该位置显示生成中指示 |
| 6 | 标准/我的要件卡片标题行右侧出现删除按钮，样式与个案要件一致，tooltip「从本案移除，不影响案由模板」 |
| 7 | 点删除：卡片消失、toast 提示、标签计数与勾选统计刷新、答案与优化记录清理 |
| 8 | 删除后「一键生成」不再生成已移除要件 |
| 9 | 后台案由要件配置与个人要件预设不受删除影响；同案由新案件模板全量复制 |
| 10 | 存在已移除要件时，抽屉底部出现「恢复默认要件」链接；点击恢复全部并 toast；无可移除要件时不显示该链接 |
| 11 | 批量生成运行期间「恢复默认要件」禁用，全部完成后恢复 |
| 12 | 抽屉整体版式、配色、组件样式与现状一致，未做视觉改版（新增按钮/链接复用现有样式） |

***

# 九、V1.2.2 新建案件流程极简

> 本章节为 V1.2.2 增量调整（V1.2.3 修订），对应 2026-09-07 演示评审《原型待调整清单》第 12 项。
> 涉及页面：pages/cases.html（新建案件弹窗、编辑弹框预填、「新」标、当事人列名）、pages/case-files.html（解析状态提示条）；js/cases.js、js/case-files.js。

## 9.1 调整背景

| # | 问题 | 现状行为 |
| :--- | :--- | :--- |
| 1 | 新建弹窗承载过多 | 弹窗含：上传区（两段说明文案）+ 案件名称必填输入 + 「补充结构化信息」折叠区（案号/案字/案由/原告/被告/承办人/上传日期共 7 字段）；实际上传场景用户核心诉求只有一个——把材料传上去开始解析 |
| 2 | 无解析过程感知 | 新建案件时材料直接标记为「已解析」（parseStatus: 'success'），无解析中状态，用户感知不到系统在做什么 |
| 3 | 结构化信息无处确认 | 名称/案由/当事人等本可由系统识别，现状要求用户上传时手工填写（选填），事后又缺少统一的确认/修改时机（见清单第 10 项背景） |

## 9.2 调整 1：新建案件弹窗极简

### 9.2.1 交互方案

- **保留**：弹窗标题「新建案件」+ **案件名称必填输入框（V1.2.4 恢复，带红色必填星标；默认值 = 首个文件名去扩展名，上传后自动填充，用户可改；移除全部文件后自动清空，可再次自动填充；为空时点「上传并解析」toast 报错「请填写案件名称」并聚焦闪烁）** + 上传区（点击/拖拽）+ 文件列表（可逐个移除）+ 底部按钮
- **移除**：
  - ~~案件名称必填输入框~~（V1.2.4 恢复：保留名称输入框，默认自动填充首个文件名，用户可改，必填星标——避免用户建案后找不到刚上传的案子）
  - 「补充结构化信息」折叠区全部 7 个字段（解析完成后由列表页「编辑」弹框承接，识别结果预填，见 9.3.3）
  - 上传区内蓝色提示条「支持压缩包或单个文件。上传压缩包可一次性归入卷宗。」等冗余说明
- **上传区文案（V1.2.3 对齐实际系统截图）**：主文案「点击或拖拽文件到此处上传」+ 两行格式提示「支持 PDF / DOC / DOCX / JPG / JPEG / PNG，以及 ZIP 整卷压缩包」「单个材料不超过 50MB，压缩包总大小不超过 1000MB」+ 胶囊提示「ⓘ 上传 ZIP 可一次性归入整卷卷宗」
- **主按钮（V1.2.3：文案固定「上传并解析」，必须 ≥1 个文件才能建案）**：
  - 未上传文件：按钮弱化置灰（可点击），悬停提示「请先上传至少 1 个材料文件」；点击不建案，toast 报错「请先上传至少 1 个材料文件，再上传并解析」+ 上传区边框橙色闪烁引导（原支持 0 文件建案，V1.2.3 取消；按钮文案不做「保存案件/上传并解析」切换）
  - 已上传 ≥1 个文件：按钮恢复主色可用，文案保持「上传并解析」
- **点击「上传并解析」**：创建案件 → 关闭弹窗 → **留在案件列表页（不跳转）**
  - 新案件置顶显示，案件名称处带**「新」标识**（生命周期见 9.3.4）
  - 案件默认信息：名称 = 弹窗名称输入框的值（默认自动填充首个文件名去扩展名，用户可改，V1.2.4）；承办人 = 当前用户；上传日期 = 当天；其余字段由解析识别预填
  - toast「已上传 N 个文件，正在解析…」；材料进入「解析中」状态（见 9.4）

### 9.2.2 保持不变

- **建案规则（V1.2.3 修订）**：必须上传 ≥1 个文件才能建案（原支持 0 文件建案，取消）；无文件点「上传并解析」仅提示引导，不建案
- **上传文件格式/大小校验（V1.2.3 对齐实际系统）**：类型 PDF / DOC / DOCX / JPG / JPEG / PNG / ZIP；单个材料 ≤ 50MB、ZIP 压缩包 ≤ 1000MB，逐文件校验并 toast 报错；文件列表图标按类型区分（压缩包/图片/文档）
- 案件创建的数据写入链路（getCurrentBusiness().cases.unshift + saveBusinessSystems）
- 同步案件弹窗、补充上传弹窗不受影响

## 9.3 调整 2：解析状态提示与信息修改入口（无确认浮层）

> 交互原则：**系统不做打断式确认**——解析状态以页面内提示条呈现，结构化信息的确认与修改由用户通过列表页「编辑」按钮主动完成。

### 9.3.1 列表页表现

- **「新」标识**：上传建案（≥1 文件）成功时，案件名称旁显示「新」小标签
- **材料数量列解析态**：解析中显示「成功数/总数」+ 转圈图标（沿用现有 parsing 态样式），完成后恢复文件图标
- **不做自动弹出任何弹框**

### 9.3.2 详情页提示条（新增，位置：标题栏下方、内容区上方，对齐实际系统截图）

**状态 1 · 解析中**（浅蓝底 + 蓝色 ⓘ 图标，无按钮，纯状态提示）：

> ⓘ 正在从材料中提取案件信息

- 显示条件：当前案件存在 `parseStatus === 'parsing'` 的文件
- 消失时机：全部文件解析完成时自动切换为状态 2

**状态 2 · 解析完成待确认**（浅黄底 + ⚠ 图标，右侧带操作按钮，提示用户确认修改）：

> ⚠ 案件信息已提取，请确认是否准确；如识别有误，可点击"去修改"调整

- 显示条件：首次解析已完成 **且** 用户尚未编辑保存过（`firstParsePending: true`）
- **【去修改】按钮（V1.2.10 调整）**：**在当前详情页内直接弹出「编辑案件信息」公共组件弹框**（详见 9.8），预填识别结果；**不再跳转列表页**
  - 调整理由：跳转会中断用户在详情页的操作上下文（已选材料、生成配置等），且用户本就在详情页，跳走再回来属多余路径
  - 弹框与列表页「编辑」按钮为**同一套组件**（见 9.8 公共组件），字段、预填规则、保存行为完全一致
- **消失时机**：用户在编辑弹框【保存】后（含详情页内弹出或从列表页进入），标记确认，此后不再显示
- 演示脚本呼应：mock 识别结果刻意演示"案由识别为上级案由"问题（如「房屋租赁合同」→「合同纠纷」，对应会议纪要实际案例），体现"提示确认修改"的价值

### 9.3.3 信息修改入口：列表页「编辑」按钮

- **沿用现有**「编辑案件信息」弹框（editDialog）与字段结构，不做新组件；对齐实际系统截图布局
- **变化：识别结果预填**——首次解析完成后点「编辑」，mock 识别结果自动预填（此前为空）：
  | 字段 | 预填与交互 |
  | :--- | :--- |
  | 案件名称 | 预填默认名（首个文件名），必填；清空后【保存】禁用 |
  | 案由 | 树形选择器（复用现有案由树弹窗），预填 mock 识别结果 |
  | 案号 / 案字 | 案号预填 mock（如「（2026）京0105民初12345号」）；案字下拉预填（如「民初」），按案件大类过滤、与案由联动（沿用现有） |
  | 原告/上诉人 / 被告/被上诉人 | 预填 mock 当事人；标签随案字切换（沿用现有） |
  | 承办人 | 预填当前用户，可改派或增加协作承办人（提示「默认是你本人，可改派或增加协作承办人」） |
  | 上传日期 | 预填建案日期（沿用现有日期控件） |
- **【保存】**：写回案件对象，关闭弹框；若为首次确认则移除「新」标、详情页提示条状态 2 不再显示
- **【取消】/ X**：不写回、不视为确认，「新」标与提示条保留

### 9.3.4 「新」标识生命周期

- **出现**：上传建案（≥1 文件）成功时
- **持续**：解析中 → 解析完成未确认（整个期间均显示，期间进出详情页不影响）
- **消失**：用户点「编辑」并【保存】（即完成信息确认）


## 9.4 数据与流程

```
列表页「新建案件」→ 极简弹窗（仅上传入口）
    ├─ 0 文件：点「上传并解析」→ toast 提示 + 上传区闪烁引导，不建案（V1.2.3：必须 ≥1 文件）
    └─ ≥1 文件：点「上传并解析」→ 建案（默认名 = 首个文件名）
          → 留在列表页，新案件置顶 + 「新」标，材料列转圈
          → 材料逐个「解析中」（每文件独立 2-3 秒 mock）
          → 全部解析完成
          → 列表页：新标保留（待确认）；详情页：提示条切换为「已提取，请确认」
          → 用户点列表页「编辑」或详情页提示条【去修改】（V1.2.10：在当前页内弹出公共组件，详见 9.8）
          → 编辑弹框识别结果预填 → 修改并【保存】
          → 「新」标消失、详情页提示条消失 → 正常使用
```

- **解析状态改造**：新建案件 files 的 `parseStatus` 由 `'success'` 改为 `'parsing'`，每个文件独立 2-3 秒 mock 解析计时（复用补充上传的 `startMockParsing` 机制），完成后写回 `'success'` 并触发既有 `case-file-parse-updated` 事件
- **解析续接**：列表页与详情页加载时均检测「parseStatus 为 parsing 的文件」并续接 mock 计时（覆盖中途刷新/切换页面场景）
- **案件标记**：建案时 `firstParsePending: true`；编辑【保存】后置 `false`。「新」标与详情页提示条状态 2 均依据该标记
- **mock 识别结果**：建案时生成并挂到案件对象（`parseResult: { caseNumber, caseWord, cause, partyA, partyB }`）；解析完成前点「编辑」不预填（parseResult 尚未"提取"），完成后预填；案由 mock 从当前业务案由库取值
- **【去修改】跳转链**：详情页 → sessionStorage 记录 `openEditCaseId` → 跳转列表页 → 列表页加载检测并自动打开对应编辑弹框

## 9.5 边界与异常 case

1. **0 文件点「上传并解析」**（V1.2.3）：不建案；toast「请先上传至少 1 个材料文件，再上传并解析」+ 上传区橙色闪烁引导 1.5 秒；按钮弱化置灰但可点击（保证有反馈）
1a. **名称为空点「上传并解析」**（V1.2.4）：不建案；toast「请填写案件名称」+ 输入框聚焦红色闪烁；名称默认自动填充（除非用户手动清空），正常流程不会触发
2. **解析中刷新或离开列表页再回来**：列表页加载时续接未完成的 mock 解析；「新」标不受影响
3. **解析中点进案件详情页**：详情页续接解析；解析中显示蓝色提示条，完成后切换为黄色确认提示条
4. **解析完成但从未点编辑**：「新」标与详情页提示条持续显示（提醒待确认），不影响其他功能使用
5. **部分文件解析异常**：正常文件解析完成即视为提取完成（识别基于可用材料），异常文件走既有解析异常处理（列表页黄色标识）
6. **编辑保存时名称为空**：【保存】禁用（名称必填），不清空回退
7. **补充上传 / 同步案件创建的案件**：不显示「新」标、无提示条、编辑不预填识别结果（parseResult 不生成）
8. **列表页解析中拦截**：沿用 v2.23 规则——解析中案件的「生成文书」置灰，不受本次调整影响

## 9.6 验收要点（V1.2.2 / V1.2.3）

| # | 验收项 |
| :--- | :--- |
| 1 | 新建弹窗含：**案件名称必填输入框（V1.2.4 恢复，红星标，默认自动填充首个文件名可改）** + 上传区 + 文件列表 + 取消 + 主按钮；无结构化信息折叠区、无冗余提示条 |
| 2 | 0 文件时主按钮「上传并解析」弱化置灰可点击：点击后 toast 报错 + 上传区闪烁引导，**不建案**（V1.2.3） |
| 3 | 上传 ≥1 文件后：名称输入框自动填充首个文件名（去扩展名），按钮恢复主色可用（文案固定「上传并解析」）；点击后弹窗关闭、**留在列表页**、新案件置顶且名称旁带「新」标、案件名为输入框值（V1.2.4） |
| 4 | 列表页新案件材料列显示解析中（转圈 + 成功数/总数），每个文件 2-3 秒逐个完成 |
| 5 | 解析中进入详情页：标题栏下方显示浅蓝提示条「正在从材料中提取案件信息」，无按钮 |
| 6 | 全部解析完成后：详情页提示条切换为浅黄「案件信息已提取，请确认是否准确…」+【去修改】按钮；列表页无任何自动弹框 |
| 7 | 点【去修改】（V1.2.10）：**在当前详情页内直接弹出**编辑弹框（公共组件 9.8），mock 识别结果已预填（案由/案号/案字/当事人/承办人） |
| 8 | 直接从列表页点「编辑」（解析完成后）：同样预填识别结果；修改后【保存】：列表页信息即时更新、「新」标消失、再进详情页不再显示确认提示条 |
| 9 | 编辑【取消】/X：不写回、不视为确认，「新」标与提示条保留 |
| 10 | 名称清空时【保存】禁用 |
| 11 | 补充上传、同步案件创建的案件：不显示「新」标与提示条、编辑不预填识别结果 |
| 12 | 解析中案件在列表页「生成文书」置灰（既有规则回归） |
| 13 | 列表页当事人列列头固定显示「当事人」（原按业务动态显示「原告/被告」等） |

## 9.7 调整 3：列表页「当事人」列名统一（小调整）

- **现状**：当事人列列头按当前业务动态显示（法院业务显示「原告/被告」，其他业务显示对应称谓），由 `partiesLabels` 驱动
- **调整**：列头**固定显示「当事人」**，不再随业务切换（对齐实际系统截图；单元格内容仍显示"A 诉 B"格式，业务称谓逻辑不变）
- **范围**：仅列表页列头文案；编辑弹框内的「原告/被告」字段标签及其随案字切换逻辑不变

## 9.8 V1.2.10 案件信息编辑弹框公共组件

> V1.2.10 新增。背景：详情页提示条【去修改】要复用与列表页「编辑」按钮**同一套弹框**，避免复制两份代码不同步；该弹框涉及案由树、案字联动、承办人拆分等逻辑，约 200 行 JS+HTML 重复成本高。

### 9.8.1 组件设计

- **位置**：`js/case-edit-dialog.js`，独立公共组件
- **依赖**：`js/case-data.js`（案由树、案字表、承办人、案件查找）+ `js/common.js`（showNotification）
- **加载顺序**：必须在 `case-data.js` 之后、调用方业务 JS 之前
- **DOM 注入**：首次调用时幂等注入到 `<body>` 末尾，所有元素 id 带 `ced_` 前缀避免与列表页原 `editDialog` 冲突
- **案由树弹窗**：组件内独占一份（不再依赖 `cases.html` 的 `#causeSelectorModal`）
- **CSS**：复用 `cases.html` 现有的 `.modal-dialog` / `.cause-selector-modal` / `.cause-tree` 等样式（`common.css` 已定义），新增组件无需引入额外 CSS

### 9.8.2 公共 API

```js
openCaseEditDialog(caseId, {
    onSaved: function (savedCaseItem) { /* ... */ }
});
closeCaseEditDialog();
submitCaseEditDialog();   // 由弹框内【保存】按钮调用
```

- `onSaved` 回调在保存成功后触发，参数为更新后的案件对象；调用方用于刷新 UI（标题栏、提示条、列表等）
- 列表页 `cases.js` 的现有 `openEditCase` / `closeEditCase` / `submitEditCase` **本次不迁移**（避免回归），后续模块再做统一（见 9.8.4）

### 9.8.3 详情页集成点

- `pages/case-files.html` 在 `case-files.js` 之前引入 `<script src="../js/case-edit-dialog.js">`
- `js/case-files.js` 的 `gotoEditCaseInfo()` 由"跳列表页 + 写 sessionStorage"改为"调用 `openCaseEditDialog(caseId, {onSaved})`"
- 保存后回调做两件事：① 重新渲染详情页提示条（因 `firstParsePending=false` 而消失）② 刷新案件标题栏（案号、案件名称等）

### 9.8.4 保持不变

- 弹框字段、预填规则、保存逻辑与列表页 `editDialog` 一致（含首次解析预填 mock 识别结果、案字联动、承办人顿号拆分等）
- 列表页编辑入口**暂保留** `cases.js` 的本地实现，本次不破坏其链路

### 9.8.5 验收要点（V1.2.10）

| # | 验收项 |
| :--- | :--- |
| 1 | 详情页解析完成后，提示条【去修改】点击后**在详情页内直接弹出**编辑弹框，**不跳转**列表页 |
| 2 | 弹框字段（名称/案号/案字/案由/原告/被告/承办人/日期）完整、预填识别结果 |
| 3 | 案由树可正常展开/选择/搜索 |
| 4 | 保存后详情页提示条消失（因 `firstParsePending=false`），标题栏案号/名称即时刷新 |
| 5 | 列表页「编辑」按钮仍可正常使用（本次不破坏） |
| 6 | 取消/ESC 均能正确关闭弹框且不写回数据 |

## 9.9 V1.2.15 管理端 OCR / 分块查看（清单 21 · 定稿：独立页面）

> **形态定稿**：管理端 `pages/admin/cases.html` 案件管理 → **操作列** → 点击进入**独立页面** `pages/admin/ocr-chunk-view.html`。
> **版本纠偏**：
> - V1.2.11 放在「文件数」列 → 列宽仅 100px，图标与数字挤压，**已撤回**
> - V1.2.12 误改为案件详情页右栏 → 该栏默认 `flex: 0 0 0%` 折叠，仅生成文书后才展开，入口实际不可见，**已废弃**
> - V1.2.13 回到管理端操作列，但用了**附件式弹框** → 用户要求「不要这种弹框，做一个新页面」，**已废弃**
> - **V1.2.15 定稿**：入口仍在管理端操作列，形态改为**独立页面**（左材料 / 右内容完整展开，非弹框）

### 9.9.1 入口

- **位置**：管理端案件列表每行**操作列**，与【编辑】【改承办人】【删除】并列，追加【OCR/分块】按钮
- **样式**：蓝色系（`color:#0369a1` / `border:#bae6fd`），与现有操作按钮视觉权重一致
- **提示文案**：「查看案件 OCR 内容与分块信息（后台排错）」
- **跳转**：`ocr-chunk-view.html?caseId=<案件ID>`（当前页跳转，非弹框）
- **可用范围**：正常案件行显示；已软删除案件的【查看】分支不提供

### 9.9.2 操作列宽度修复（同版本）

- 原 `BASE_COL_TEMPLATE_RIGHT` 末项操作列宽 **180px**，容纳【编辑】【改承办人】【删除】【OCR/分块】四个按钮时被挤压/截断
- 调整为 **268px**，`.case-grid .action-cell` 增加 `flex-wrap: nowrap; white-space: nowrap; align-items:center;`
- 同步位置：`js/admin-cases.js` 的 `BASE_COL_TEMPLATE_RIGHT`；`pages/admin/cases.html` grid 的 `--case-grid-cols` 硬编码兜底值

### 9.9.3 新页面布局（左材料 / 右内容）

页面复用管理端标准布局（左侧固定侧边栏 + 顶部面包屑「首页 / 案件管理 / OCR/分块查看」+ 页面标题与【返回案件列表】按钮），主体左右两栏铺满视口：

| 区域 | 内容 |
| :--- | :--- |
| **左栏（260px）** | 案件材料列表：每条含解析状态圆点（绿=success / 橙=parsing / 红=error）+ 文件名；点击切换查看对象，当前项高亮 |
| **右栏（自适应）** | ① 信息条：文件名 · 解析状态徽标 · 错误类型 errorType · 解析时间 · 页数<br>② 子 Tab：【识别全文】/【分块预览】<br>③ 识别全文：OCR 文本 + 底部「共 N 字 · K 段」<br>④ 分块预览：粒度单选（按页/按段/按句）+【重新切片】，块卡片显示 序号/粒度/起止位置/摘要 |

- **默认行为**：进入页面后自动选中第一个材料文件并加载其 OCR 内容
- **无材料**：左栏显示「暂无材料」，右栏提示「该案件暂无材料」
- **缺少/无效 caseId**：提示「未找到该案件（caseId：xxx），请从案件列表重新进入」

### 9.9.4 数据来源

- 新页面引入 `js/case-data.js`，固定法院业务系统（`currentBusiness='court'`），按 URL `caseId` 在 `businessSystems.court.cases` 中查找案件
- 不改动 `startMockParsing` / `case-data.js` 文件结构
- 现场生成 mock OCR 文本（`buildMockOcrText`）+ 三粒度切分（`sliceOcrText`），实现位于 `js/ocr-chunk-view.js`（与旧弹框逻辑一致）
  - 文本含「原告…」「被告…」「本院经审理查明…」「本院认为…」及 `[page]第 N 页` 标记
  - 按页切：按 page 标记；按段切：按空行；按句切：按句号
- **解析中与异常文件同样可查看**（排错场景正是要看这些）
- 仅内存展示，**不持久化**

### 9.9.5 用户侧清理

- **移除** `pages/case-files.html` 中 V1.2.12 残留的 OCR 模式 Tab（`#resultModeTabs`）、OCR 排错区（`#ocrChunkPane`）及其全部 CSS
- **移除** `js/case-files.js` 中 V1.2.12 的 OCR 逻辑块（`switchResultMode` 的 OCR 分支、`populateOcrFileSelect`、`onOcrFileChange`、`loadOcrChunkForCurrentFile`、`switchOcrSubTab`、`renderOcrInfo`、`renderOcrText*`、`renderOcrChunks`、`setOcrChunkGranularity`、`rerunOcrChunkSlice`、`buildMockOcrText`、`sliceOcrText`）
- 用户侧案件详情页**不再有任何 OCR 入口**；OCR 查看能力仅在管理端（排错用）

### 9.9.6 保持不变

- 管理端列表、列配置、筛选、分页、批量操作、编辑、改承办人、删除/恢复、材料树、文书列表、内容预览均不变
- 文件对象字段（parseStatus / ocrStatus / parsedAt / errorType）不变
- `case-data.js` 数据结构与 mock 生成逻辑不变

### 9.9.7 验收要点（V1.2.15）

| # | 验收项 |
| :--- | :--- |
| 1 | 管理端案件列表操作列【编辑】【改承办人】【删除】【OCR/分块】四个按钮**完整可见不截断** |
| 2 | 点击【OCR/分块】**跳转到独立页面**（非弹框），URL 带 `caseId` |
| 3 | 新页面左侧为管理端侧边栏 + 面包屑「首页 / 案件管理 / OCR/分块查看」 |
| 4 | 左栏列出该案件全部材料，带解析状态圆点（绿/橙/红），当前项高亮 |
| 5 | 进入时默认选中第一个材料并加载 OCR 内容 |
| 6 | 点击左栏其他材料，右侧内容切换为该材料的 OCR |
| 7 | 右栏信息条正确显示状态徽标与 errorType |
| 8 | 【识别全文】显示 OCR 文本 + 字数/段数统计 |
| 9 | 【分块预览】显示分块卡片，粒度切换（按页/按段/按句）立即重新切分 |
| 10 | 【重新切片】点击后 loading 600ms 再展示新结果 |
| 11 | 【返回案件列表】可回到管理端案件列表 |
| 12 | 用户侧案件详情页**不再出现**「OCR 排错」Tab 及相关区域 |
| 13 | 管理端原有功能（列表/筛选/分页/编辑/删除/材料树/文书列表）全部正常 |

***

# 十、V1.2.4 文书生成流程重构（法条确认前置）

> 本章节为 V1.2.4 增量调整，对应 2026-09-07 演示评审《原型待调整清单》第 1/2/3/4 项。
> 涉及页面：pages/case-files.html（新增「本案法条」入口与抽屉、渲染样式）；js/case-files.js（生成链路、法条异步任务、渲染函数）。
> 核心决策：**一次动作只允许一次模型调用**。原「点一次生成 = 一次调用直接出全文」拆为「第一次调用出法条 → 用户确认 → 第二次调用出文书」。

## 10.1 调整背景

| # | 问题 | 现状行为 |
| :--- | :--- | :--- |
| 1 | 一次动作内多次模型调用 | 点「生成文书」后由单一流程一次性完成检索法条 + 生成全文，耗时长、过程不可控 |
| 2 | 法条不可见不可改 | 法条由流程内部消化，用户既看不到引用了哪些法条，也无法在生成前干预；存在虚构/错配风险 |
| 3 | 大模型渲染质量差 | 实测耗时约 2 分钟，存在丢内容、篡改话术问题 |
| 4 | 缺少可等待的异步感知 | 用户点生成后只能干等，无任何中间产出可查看 |

## 10.2 调整 1：工作流拆分为「法条确认 → 文书生成」两段

### 10.2.1 新流程

```
案件上传完成（材料解析触发，与结构化信息提取同源）
    └─ 后台异步启动法条检索（mock 4-6s）  ← 不占用用户动作
          │
进入案件详情页 → 检测：未完成则等待 / 已完成则直接可用
          │
点「生成文书」（一步生成 / 分步生成最终「生成文书」）
    │
    ├─ 前置校验（案件阶段 / 材料选择 / 解析状态 / workflow 超限）… 不变
    │
    ├─ 要件引入（仅 judgment 且该案由有可用要件）… V1.2 流程不变
    │     └─ 要件抽屉【确认生成（引入 N 条要件）】
    │
    ▼
本案法条抽屉 · 生成模式（全部文书类型必经）
    ├─ 顶部后果说明：「勾选的法条将写入文书『法律适用依据』章节，未勾选的不写入」
    ├─ 核心依据（2-3 条，默认勾选）/ 可补充依据（默认不勾）/ 手动添加（默认勾选）
    ├─ 法条已就绪 → 直接展示，可改/可增删/可勾选
    ├─ 法条未就绪 → 抽屉内显示「正在检索适用法条…」加载态，完成后自动填充
    └─ 底部【确认法条并生成文书（N 条写入文书）】
          │
          ├─ N ≥ 1：抽屉关闭 → 主界面脚本流式渲染开始（仅写入已勾选法条）
          └─ N = 0：toast「请至少确认 1 条适用法条」，不触发生成
          │
          ▼
    脚本匹配流式渲染（标题 + 统一字体正文、靠左）
```

### 10.2.2 触发规则

- **适用全部文书类型**（判决书、庭审提纲、其他）：点「生成文书」后一律先进入法条确认环节，不再按文书类型区分
- **双入口一致**：一步生成 `generateByMaterial`、分步生成最终 `compileSteps` 均走同一法条确认链路
- **列表页快捷生成**：跳转详情页后自动引入要件并直接进入法条确认，**不自动跳过**（与要件环节处理一致：法条是法律适用依据，必须经用户确认）
- **要件与法条的先后**：要件在前（事实基础）、法条在后（法律依据）。要件抽屉【确认生成】点击后关闭要件抽屉、紧接着打开法条抽屉，用户感知为连续两步

### 10.2.3 保持不变

- 生成前置校验（案件阶段、材料选择、全部文件异常阻拦、workflow 超限弹框）全部不变
- V1.2 要件询问弹框与要件抽屉交互（含 V1.2.1 并行生成、个案移除、恢复默认）完全不变
- 要件答案的结构化数据传递方式不变

## 10.3 调整 2：法条异步预生成

### 10.3.1 触发与状态机

- **触发时机**：**案件上传后**（材料解析启动时，与结构化信息提取同源、同生命周期）即后台异步启动，不弹窗、不阻塞、不占用用户操作；案件详情页 / 列表页加载时仅负责**检测并续接**未完成任务（刷新 / 切页场景），不再作为触发点
- **状态机**：

| 状态 | 含义 | 入口按钮表现 | 抽屉内表现 |
| :--- | :--- | :--- | :--- |
| `idle` | 未启动 | 「本案法条 -」 | — |
| `running` | 检索中 | 「本案法条」+ 转圈图标 | 「正在检索适用法条…」加载态 + 进度文案 |
| `done` | 已完成 | 「本案法条 N」 | 法条清单，可改/增删/勾选 |
| `failed` | 检索失败（mock 不触发，预留） | 「本案法条 !」 | 错误提示 + 【重新检索】按钮 |

- **耗时**：mock 4-6 秒（对齐"等待可感知但不漫长"的演示节奏）
- **持久化**：`localStorage.caseLaws_${caseId}` = `{ status, cause, items: [...] }`
  - `cause` 记录检索时的案由，用于失效判断（见 10.3.3）
- **续接**：页面加载时若 `status === 'running'` 则续接 mock 计时（覆盖中途刷新/切页场景）

### 10.3.2 法条数据来源（mock）

- 按案件**案由关键词**匹配法条库，每次返回 4-8 条：

| 案由关键词 | 法条示例 |
| :--- | :--- |
| 借贷 | 民法典第 667 / 674 / 676 / 680 条，民诉法第 147 / 260 条 |
| 租赁 | 民法典第 703 / 708 / 712 / 722 / 724 条 |
| 买卖 | 民法典第 595 / 626 / 577 条 |
| 劳动 | 劳动合同法第 30 / 38 / 46 / 82 条 |
| 侵权 / 人身损害 | 民法典第 1165 / 1179 / 1183 条 |
| 兜底（其他） | 民法典第 7 条（诚信）、第 577 条（违约责任），民诉法第 67 条（举证责任）、第 147 / 260 条 |

- 每条法条结构：`{ id, name, content, source: 'ai' | 'manual' }`
  - `name` 例：「《中华人民共和国民法典》第六百六十七条（借款合同定义）」
  - `content` 例：「借款合同是借款人向贷款人借款，到期返还借款并支付利息的合同。」
- 默认全部勾选（`selected: true`）

### 10.3.3 失效与重跑

- **案由变更**：详情页修改案由后，若 `caseLaws.cause !== 新案由`，清空**全部法条数据**（含手动添加，因案由已变、原法条不再适用）并自动重新异步检索（对应清单第 10 项"案由识别错导致匹配错"的连锁修复）
- **手动重跑（【重新检索】按钮）**：仅重新生成 **AI 推荐**法条，**手动添加的法条予以保留**。点击有确认提示（文案「重新检索将按当前案由重新生成 AI 推荐法条，手动添加的法条将保留，是否继续？」），避免误触丢失用户已编辑的 AI 推荐内容；完成后新 AI 推荐追加在原有手动法条之后
- **要件/法条的联动**：改案由时**保留要件答案**（用户可能已编辑），仅重跑法条（评审确认方案）

## 10.4 调整 3：「本案法条」抽屉（复用要件抽屉）

> 设计原则：**与「本案要件」抽屉完全一致的一套交互**，用户零学习成本；代码复用 `case-elements-*` 样式体系。

### 10.4.1 入口

- 材料树顶部固定条，与「本案要件 N」并列新增按钮：

```
┌────────────────────────────────────────┐
│ 🧩 本案要件 16  ›                        │
│ ⚖ 本案法条 6   ⟳   ← 新增（检索中转圈）  │
└────────────────────────────────────────┘
```

- 点击打开「本案法条」抽屉；与要件抽屉互斥（同一时刻只打开一个）

### 10.4.2 抽屉结构

| 区域 | 内容 |
| :--- | :--- |
| 头部 | ⚖ 图标 + 「本案法条」+ 关闭按钮 |
| 统计条 | 「共 N 条 · 已选 M 条」+ 手动添加计数 |
| 后果说明条 | 「勾选的法条将写入文书『法律适用依据』章节，未勾选的不写入」 |
| 列表 | 分三组：**核心依据 / 可补充依据 / 手动添加**；每条法条一个卡片：勾选框 + 法条名称（可内联编辑）+ 法条内容（可内联编辑）+ 层级或来源标签 + 删除按钮 |
| 底部 | 【添加法条】【重新检索】，生成模式下另加主按钮【确认法条并生成文书（N 条写入文书）】 |

- **默认勾选（V1.2.8）**：核心依据 ✅ 勾选、可补充依据 ⬜ 不勾、手动添加 ✅ 勾选；详见 10.10
- **层级/来源标签**：核心 = 蓝 / 可补充 = 灰蓝 / 手动添加 = 橙
- **添加法条**：底部【添加法条】打开法条库弹窗（搜索勾选 / 新建自定义法条），保存后加入列表并默认勾选（见 10.9）
- **重新检索**：仅重新生成 AI 推荐法条，手动添加的法条保留（见 10.3.3）
- **删除法条**：卡片右侧删除按钮，直接移除 + toast「已移除 1 条法条」，不弹二次确认（与要件个案移除一致）
- **内联编辑**：点击名称或内容进入编辑态，失焦/回车保存，Esc 取消

### 10.4.3 生成模式 vs 日常模式

| | 生成模式 | 日常模式 |
| :--- | :--- | :--- |
| 打开方式 | 生成流程中自动打开 | 材料树「本案法条」按钮 |
| 底部主按钮 | 【确认法条并生成文书（N 条）】 | 不显示 |
| 关闭行为 | 放弃本次生成，回到配置区 | 仅关闭，编辑内容保留 |
| 编辑持久化 | 实时写入 localStorage | 同 |

### 10.4.4 确认校验

| 校验情形 | 行为 |
| :--- | :--- |
| N ≥ 1 | 关闭抽屉 → 开始脚本流式渲染；上下文 = 已勾选法条（结构化数组） |
| N = 0（全部取消勾选） | toast「请至少确认 1 条适用法条」，不触发生成 |
| 法条仍在检索中 | 主按钮禁用 + 文案「法条检索中…」，检索完成后自动恢复 |

### 10.4.5 生成中状态约束

- 渲染期间「本案法条」「本案要件」两个入口**均禁用**，hover 提示「文书生成中，暂不可修改」
- 渲染结束后恢复

## 10.5 调整 4：渲染方案改为脚本匹配

> 决策：弃用大模型渲染（实测 ~2 分钟、丢内容、篡改话术），改**脚本匹配式流式渲染**。本期只做「标题 + 统一字体正文、靠左」，案号 / 签章 / 特殊格式**本期不做**，格式模板不引入。

### 10.5.1 渲染规范

| 元素 | 规范 |
| :--- | :--- |
| 文书标题 | 16px、加粗、颜色 `#1f2937`、**居左**、上下留白 16px |
| 正文 | 统一 14px / 行高 1.8、颜色 `#374151`、首行缩进 2 字符、**整体靠左** |
| 段落间距 | 12px |
| 小标题（如有） | 15px 加粗、`#1f2937`、居左 |
| 案号 / 签章 / 落款日期 | **本期不输出**（后置） |

### 10.5.2 流式行为

- 沿用现有 `startStreamingOutput` 两段式（思考过程 → 正文分段），**思考过程精简为 3 条**（原 6 条）：
  1. 正在阅读案件材料，梳理案情脉络…
  2. 结合已确认法条，组织文书结构…
  3. 生成正文内容…
- 正文按段实时追加，进度条显示「已输出 X 字」
- **移除**：原 `result-doc` 的居中标题、meta 信息行（案件/生成时间）、右对齐签章行、右对齐日期行
- **移除**：原「套格式骨架」（`applyFormatSkeleton` / `getSignerLabel`）在本期渲染中的调用——保留函数供后续格式渲染复用

### 10.5.3 保持不变

- 流式输出期间【文书精修】【重新配置】按钮禁用逻辑
- 输出完成后的历史文书保存、文书精修入口
- 流式进度条与字数统计组件

## 10.6 数据与流程

```
案件上传完成 → 后台启动法条异步检索（status: running）
   （与结构化信息提取同源，随材料解析一并触发）

详情页 / 列表页加载（initPage / init）
  └─ 检测 localStorage.caseLaws_${caseId}
        ├─ 无 → 启动异步检索（status: running）
        ├─ running → 续接计时
        └─ done 且 cause 未变 → 直接可用

点「生成文书」
  → 前置校验 → [要件环节（judgment）] → 打开本案法条抽屉（生成模式）
  → 用户确认 → 收集已勾选法条 → 关闭抽屉
  → startStreamingOutput（脚本渲染）

详情页修改案由
  → 比对 caseLaws.cause
     ├─ 一致 → 不动
     └─ 不一致 → 清空 + 重新异步检索 + toast「案由已变更，正在重新检索适用法条」
```

- **数据结构**：
  - `localStorage.caseLaws_${caseId}` = `{ status: 'idle'|'running'|'done'|'failed', cause: string, items: [{ id, name, content, source, selected }] }`
  - 生成上下文以**结构化数组**传递：`[{ name, content }]`，范围 = 已勾选法条
- **历史数据**：无存量，不需要迁移

## 10.7 边界与异常 case

1. **上传后立即点生成**（法条仍在检索）：抽屉显示加载态，主按钮禁用；检索完成后自动填充并恢复按钮
2. **检索中刷新页面**：续接计时，不重复启动
3. **法条全部取消勾选点确认**：toast 提示，不生成
4. **生成模式下关闭抽屉（X / 遮罩）**：放弃本次生成，回到配置区；已编辑法条保留
5. **放弃后再次点生成**：重新打开法条抽屉，保留上次编辑结果
6. **改案由时法条正在检索**：取消原计时器，按新案由重新检索
7. **手动添加法条后点【重新检索】**：仅重建 AI 推荐法条，手动添加的法条保留（确认弹框已说明）
8. **分步生成中途返回**：不影响法条数据（已持久化）
9. **渲染期间点材料树入口**：两个入口均禁用，hover 提示
10. **法条检索 mock 失败**（预留）：状态置 `failed`，抽屉显示错误 + 【重新检索】

## 10.8 验收要点（V1.2.4）

| # | 验收项 |
| :--- | :--- |
| 1 | 案件上传完成后，材料树顶部「本案法条」入口初值显示检索中转圈，4-6 秒后显示条数；进入详情页时未完成则继续等待、已完成则直接展示 |
| 2 | 点入口打开「本案法条」抽屉：法条清单可勾选、名称与内容可内联编辑、可新增、可删除 |
| 3 | 点「生成文书」（任意文书类型）：先走要件环节（judgment 时），再自动打开法条抽屉（生成模式） |
| 4 | 法条检索未完成时打开抽屉：显示加载态，主按钮禁用；完成后自动填充并恢复 |
| 5 | 底部显示【确认法条并生成文书（N 条）】，N 随勾选实时联动 |
| 6 | 全部取消勾选点确认：toast「请至少确认 1 条适用法条」，不生成 |
| 7 | N ≥ 1 点确认：抽屉关闭，主界面开始流式渲染 |
| 8 | 分步生成完成全部步骤后点「生成文书」，同样进入法条确认环节 |
| 9 | 渲染结果：标题居左加粗、正文统一 14px/1.8 居左、无居中标题、无 meta 行、无右对齐签章与日期 |
| 10 | 渲染期间「本案法条」「本案要件」入口均禁用并 hover 提示，结束后恢复 |
| 11 | 详情页修改案由 → toast 提示重跑法条，法条数据按新案由重新检索，要件答案保留 |
| 12 | 渲染完成后【文书精修】入口正常，历史文书保存链路不受影响 |

## 10.9 V1.2.5 法条分类：AI 推荐 / 手动添加（库内检索）

> 本节为 V1.2.5 增量调整。背景：原「系统检索」单一来源既无法区分"系统推荐的"与"用户主动补充的"，也缺少高效的补充手段——用户想补法条只能手打，效率低且易录错。

### 10.9.1 法条分类定义

| 分类 | 来源 | 来源标签 | 说明 |
| :--- | :--- | :--- | :--- |
| **AI 推荐** | 系统按案由自动检索 | 蓝 | 案件上传后后台异步生成；可改、可删，删除仅影响本案，不影响法条库 |
| **手动添加** | 用户从法条库检索添加 | 橙 | 从全量法条库搜索勾选；库内无匹配时可新建自定义法条（地方法规、库外条款） |

- 数据结构 `source` 取值由 `system / manual` 调整为 **`ai / manual`**；读取历史数据时 `system` 自动映射为 `ai`，无需迁移

### 10.9.2 全量法条库

- `LAW_LIBRARY_ALL`：82 条（数据源文件 `js/law-library.js`），条目结构 `{ id, category, name, content }`
- 分类（`category`）：民法典·总则 / 民法典·合同 / 民法典·物权 / 民法典·侵权 / 民法典·婚姻家庭继承 / 民事诉讼法 / 劳动合同法 / 公司法 / 刑法 / 刑事诉讼法 / 行政诉讼法
- **AI 推荐规则** `LAW_RECOMMEND_RULES`：按案由关键词映射到法条 id 列表，**实体法在前、程序法在后**，每条规则返回 6-8 条
  - 覆盖：借贷·借款 / 租赁 / 买卖 / 劳动·劳务 / 侵权·人身损害 / 交通事故 / 医疗损害 / 婚姻·离婚·抚养 / 继承 / 物权·相邻 / 刑事 / 行政 / 公司·股东 / 执行；末项兜底（诚信原则 + 违约责任 + 举证责任 + 缺席判决 + 迟延履行）
  - 规则结构（V1.2.8）：`{ keys: [...], core: [id...], optional: [id...] }`——`core` 为实体法核心条款（默认勾选，2-3 条），`optional` 为程序法与通用条款（默认不勾，≤5 条）；`buildLawItems` 依此生成 `tier` 字段
  - 覆盖：借贷·借款 / 租赁 / 买卖 / 劳动·劳务 / 侵权·人身损害 / 交通事故 / 医疗损害 / 婚姻·离婚·抚养 / 继承 / 物权·相邻 / 刑事 / 行政 / 公司·股东 / 执行；末项兜底（诚信原则 + 违约责任 + 举证责任 + 缺席判决 + 迟延履行）
  - 每条规则 **core 2-3 条 + optional ≤5 条、总数 ≤8 条**（V1.2.8 收敛，原借贷 10 条）；具体规则按**从具体到一般**排列（如"交通事故"排在"侵权"之前），取第一条命中的规则
  - 分层规则与默认勾选策略详见 10.10
- 法条正文以现行有效文本为准，不做演绎改写

### 10.9.3 添加法条交互

- **入口**：法条抽屉底部【+ 添加法条】→ 打开「法条库」弹窗（居中 modal，复用现有 `modal-overlay` / `modal-dialog` 样式）
- **弹窗结构**：
  - 顶部搜索框：按法条名称 / 正文关键词**实时过滤**（输入即筛，无按钮）
  - 结果列表：每条含 勾选框 + 法条名称 + 法条内容 + 分类标签
  - 底部：【取消】【确认添加（N 条）】，N 随勾选实时联动，N = 0 时主按钮禁用
- **防重**：已在「本案法条」中的条目显示「已添加」标记，勾选框禁用，不可重复添加
- **空状态**：无匹配结果时显示「未找到匹配的法条」+【新建自定义法条】按钮
- **新建自定义法条**：展开弹窗内内联表单（法条名称必填 / 法条内容选填 / 分类选填，默认「自定义」），保存后以 `manual` 来源加入本案并默认勾选
- **添加完成后**：关闭弹窗 → 抽屉按分组刷新 → 入口计数、统计条、已选计数联动 → toast「已添加 N 条法条」
- **移除**：两类条目均可删除，仅从本案移除，法条库不受影响

### 10.9.4 分组渲染

- 抽屉列表按来源分两组，复用要件抽屉分组样式（`.case-elements-group` / `.case-elements-group-title`）：

```
AI推荐  6 项
  ☑ 《中华人民共和国民法典》第六百六十七条（借款合同定义）   [AI推荐]
  ☑ 《中华人民共和国民事诉讼法》第六十七条（举证责任）       [AI推荐]
手动添加  2 项
  ☑ 《XX省物业管理条例》第十二条                              [手动添加]
```

- **「手动添加」组无内容时不渲染该组**（避免出现空分组占位）
- 两组内条目交互完全一致：勾选、双击内联编辑（名称 / 内容）、删除

### 10.9.5 保持不变

- 法条异步预生成状态机与持久化结构（仅在读取时做 `system → ai` 兼容映射）
- 法条确认校验规则（N ≥ 1 才能触发生成；N = 0 拦截并 toast）
- 生成中禁用入口、渲染期间两个入口均禁用
- 案由变更触发法条重跑的逻辑

### 10.9.6 验收要点（V1.2.5）

| # | 验收项 |
| :--- | :--- |
| 1 | 法条抽屉列表分为「AI推荐」「手动添加」两组，组标题带数量；无手动法条时不显示该组 |
| 2 | AI 推荐条目标签显示「AI推荐」（蓝），手动添加条目显示「手动添加」（橙） |
| 3 | 底部【+ 添加法条】打开法条库弹窗，展示全量法条（含分类标签） |
| 4 | 搜索框输入关键词（如「借款」）实时过滤结果列表 |
| 5 | 已在本案的法条显示「已添加」且勾选框禁用，无法重复添加 |
| 6 | 勾选 2 条点【确认添加（2 条）】：弹窗关闭、抽屉「手动添加」组新增 2 条、默认勾选、toast 提示 |
| 7 | 搜索无结果时显示空状态与【新建自定义法条】，填写名称后可保存并加入本案 |
| 8 | 手动添加的条目可勾选、可双击编辑、可删除，与 AI 推荐条目交互一致 |
| 9 | 手动添加的法条计入【确认法条并生成文书（N 条）】计数，并写入文书「法律适用依据」章节 |
| 10 | 删除 AI 推荐条目后不影响法条库，重新检索可再次获取 |

## 10.10 V1.2.8 法条分层与默认勾选策略

> 本节为 V1.2.8 增量调整。背景：V1.2.4 起 AI 推荐法条**默认全部勾选**，演示与评审中暴露两个问题——
> **① 后果不可见**：勾选项与生成结果之间缺因果说明，用户不知道"勾选"即意味着"写入文书的法律适用依据章节"，面对一排已勾选的框既不知会发生什么、也不知不勾会怎样；
> **② 精简成本过高**：借贷案由推荐 10 条，用户只要 3 条就得做 7 次取消勾选——默认全选把"减法"变成了主要操作，而做减法比做加法更费劲。

> **注（V1.2.9）：本节的分层 UI 已回退**——原「核心依据 / 可补充依据」分组与标签取消，AI 推荐合并为一组；默认勾选改为**全不选**。回退理由见 10.11.1。本节保留作为决策记录，以 10.11 为准。

### 10.10.1 分层定义

AI 推荐法条按重要度拆为两层，与「手动添加」并列共 **3 个分组**：

| 分组 | 来源 | 默认勾选 | 标签 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **核心依据** | AI 推荐 | ✅ **勾选** | 核心 | 2-3 条实体法核心条款（如借贷：667 定义 / 676 逾期 / 680 利率限制） |
| **可补充依据** | AI 推荐 | ⬜ 不勾选 | 可补充 | 程序法与通用条款（如举证责任、缺席判决、迟延履行） |
| **手动添加** | 用户从法条库添加 | ✅ 勾选 | 手动添加 | 用户主动添加，默认即为所需 |

- 数据结构新增 `tier` 字段：`'core' | 'optional'`（手动添加项无 tier，`source: 'manual'`）
- **历史数据兼容**：读取时无 `tier` 的 AI 推荐项按 `core` 处理，勾选状态沿用原值

### 10.10.2 后果可见（两项改造）

1. **抽屉顶部后果说明条**（统计条下方，浅灰底 + ⓘ 图标，12px 次要文字）：

   > ⓘ 勾选的法条将写入文书「法律适用依据」章节，未勾选的不写入

2. **确认按钮文案带后果**：由「确认法条并生成文书（N 条）」改为
   **「确认法条并生成文书（N 条写入文书）」**，N 随勾选实时联动

### 10.10.3 推荐数量收敛

- 每条推荐规则拆为 `core`（2-3 条）+ `optional`（≤5 条），**总数 ≤8 条**
- 收敛后各案由推荐量：借贷 8 / 租赁 8 / 买卖 8 / 侵权 7 / 劳动 7 / 交通事故 7 / 离婚 6 / 继承 5 / 医疗损害 5 / 相邻物权 5 / 刑事 4 / 公司 4 / 抵押质押 4 / 高空抛物 4 / 行政 2 / 兜底 5
- 目的：贴近实际裁判文书引用量（通常 3-8 条），直接降低筛选负担

### 10.10.4 用户操作路径变化

| | 调整前（V1.2.4-V1.2.7） | 调整后（V1.2.8） |
| :--- | :--- | :--- |
| 默认态 | 10 条全勾选 | 核心 3 条勾选，其余不勾 |
| 只要核心 3 条 | 需取消 7 次勾选 | **无需操作** |
| 需要 5 条 | 需取消 5 次勾选 | 勾选 2 次 |
| 全部需要 | 无需操作 | 需勾选 5 次（或后续考虑加「全选」） |
| 后果认知 | 无说明 | 顶部说明条 + 按钮文案明示 |

> 权衡：全都要的场景下用户操作变多，但属低频；高频场景（只想要核心几条）成本降为 0。

### 10.10.5 保持不变

- 法条确认校验（N ≥ 1 才能生成；N = 0 拦截并 toast「请至少确认 1 条适用法条」）
- 手动添加法条默认勾选、可编辑可删除
- 异步预生成状态机、案由变更重跑、【重新检索】保留手动法条
- 法条库检索弹窗交互

### 10.10.6 验收要点（V1.2.8）

| # | 验收项 |
| :--- | :--- |
| 1 | 抽屉内为「核心依据」「可补充依据」「手动添加」三组，组标题带数量；「可补充依据」无内容时不渲染该组 |
| 2 | 进入时核心依据条目默认勾选，可补充依据条目默认不勾选，手动添加条目默认勾选 |
| 3 | 统计条显示「共 N 条 · 已选 M」且与勾选实时联动 |
| 4 | 抽屉顶部显示后果说明条「勾选的法条将写入文书『法律适用依据』章节，未勾选的不写入」 |
| 5 | 确认按钮文案为「确认法条并生成文书（N 条写入文书）」，N 实时联动 |
| 6 | 借贷案由推荐总数 ≤8 条（核心 3 + 可补充 5） |
| 7 | 只保留核心 3 条直接点确认：文书「法律适用依据」章节恰好写入 3 条 |
| 8 | 勾选 2 条可补充依据后确认：写入 5 条 |
| 9 | 全部取消勾选点确认：toast「请至少确认 1 条适用法条」，不生成 |
| 10 | 历史数据（无 tier 字段）加载正常，AI 推荐项按核心处理 |

## 10.11 V1.2.9 法条交互回退与入口区分

> 本节为 V1.2.9 增量调整，**覆盖 10.10**。

### 10.11.1 调整背景（三条）

| # | 问题 | 说明 |
| :--- | :--- | :--- |
| 1 | 分层是系统在替用户判断重要性 | 10.10 的「核心依据 / 可补充依据」由系统标注哪些是核心。但**系统认为的核心也许是错的，而用户手动添加的才可能是真正的核心**——重要性应由用户判断，不由系统预设 |
| 2 | 底部按钮文案有歧义 | 「添加法条」「重新检索」用户难以分辨差异；二者本质是**人工** vs **AI** 两种获取方式 |
| 3 | 必办与选办入口 UI 相同 | 本案法条是**生成文书前必须确认**的环节，本案要件是**可选引入**；两者入口并列于材料树且样式一致，用户无从分辨轻重 |

### 10.11.2 取消分层

- 「核心依据 / 可补充依据」合并为 **「AI推荐」** 一组，不再分组、不再打核心/可补充标签
- `tier` 字段**保留但退化**：仅用于推荐结果排序（core 排前），不参与分组、不打标签、不影响默认勾选
- 来源标签恢复为：**AI推荐 = 蓝 / 手动添加 = 橙**
- 抽屉分组由 3 组（核心 / 可补充 / 手动添加）收敛为 2 组（**AI推荐 / 手动添加**）

### 10.11.3 默认全不选 + 全选/清空

- **AI 推荐法条默认全部不勾选**——不替用户预选，由用户自行判断哪些适用
- 统计条右侧提供 **【全选】【清空】** 快捷操作：想直接采用系统推荐则一键全选，想自己挑则逐条勾选
- **手动添加**的法条仍默认勾选（用户主动添加即为所需）

### 10.11.4 已勾选法条置顶

- 组内**已勾选法条置顶展示**、未勾选的在后，同状态下保持原相对顺序
- 已勾选条目加**浅色底标识**，视觉上形成"已选区"
- 勾选 / 取消后列表**即时重排**，用户勾完可立刻在顶部看到自己选了什么，无需在列表中查找
- 手动添加组同样适用（因其默认勾选，通常整体位于组顶）

### 10.11.5 底部按钮文案

| 调整前 | 调整后 | 语义 |
| :--- | :--- | :--- |
| 【添加法条】 | **【手动检索】** | 人工方式：打开法条库弹窗检索并添加 |
| 【重新检索】 | **【AI推荐】** | AI 方式：按当前案由重新生成 AI 推荐法条 |

- 二者构成 **人工 vs AI** 的清晰对立；【AI推荐】按钮 hover 提示「按当前案由重新生成 AI 推荐法条，手动添加的法条将保留」

### 10.11.6 入口区分（必办 vs 选办）

材料树顶部两个入口做视觉权重区分：

| 入口 | 性质 | 样式 | 徽标 |
| :--- | :--- | :--- | :--- |
| **本案法条** | 生成文书**必须确认** | 主色实心边框、主色文字（保持现有强调样式） | 状态徽标：**检索中**（灰转圈）/ **待确认**（橙）/ **已确认 N 条**（绿） |
| **本案要件** | **可选**引入 | 弱化样式：浅灰底、灰边框、次要文字色 | 固定灰标「**选填**」 |

- 法条徽标状态流转：进入时若检索中 → 「检索中」；检索完成未确认 → 「待确认」（橙）；用户点【确认法条并生成文书】后 → 「已确认 N 条」（绿）；案由变更或重新 AI 推荐后重置为「待确认」
- 数据：`caseLawsState.confirmed`（布尔）记录是否已确认，随 localStorage 持久化

### 10.11.7 验收要点（V1.2.9）

| # | 验收项 |
| :--- | :--- |
| 1 | 抽屉内仅「AI推荐」「手动添加」两组，无核心/可补充分组与标签 |
| 2 | AI 推荐法条默认全部未勾选；手动添加法条默认勾选 |
| 3 | 统计条右侧有【全选】【清空】，点击后勾选状态与计数同步刷新 |
| 4 | 勾选某条后该条置顶、加浅色底；取消勾选后回落，列表即时重排 |
| 5 | 底部按钮为【手动检索】【AI推荐】，点击分别打开法条库弹窗 / 重新生成 AI 推荐 |
| 6 | 材料树「本案法条」入口带状态徽标：检索中→待确认→已确认 N 条 |
| 7 | 材料树「本案要件」入口为弱化样式 + 「选填」灰标，与法条入口视觉明显不同 |
| 8 | 确认生成后法条徽标变绿「已确认 N 条」；重新 AI 推荐后重置为「待确认」 |
| 9 | 全部未勾选时点确认：toast「请至少确认 1 条适用法条」，不生成 |
